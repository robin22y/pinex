// Temporary stub. The full /iqjet implementation lives in working-tree
// scratch (src/components/iqjet/*, etc.) and will replace this stub
// in a follow-up commit. Stub exists so App.jsx's /iqjet route resolves
// and the CI build doesn't fail while /iqjet-desk ships standalone.
export default function IQjet() {
  return <div>Coming soon</div>
}
