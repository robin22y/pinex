# Copy to `.env` or `.env.local` and fill in (never commit secrets).
# Vite exposes only variables prefixed with VITE_ to client code.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
VITE_GOOGLE_CLIENT_ID=
VITE_ADSENSE_CLIENT=
VITE_SUPERADMIN_EMAIL=
